# scharade-game


```
poetry init

...

poetry add <packages>

# add folder with same name as package-name but with underscore
# scharade-game -> scharade_game

# this create .venv and stuff
poetry install

poetry install --sync
```